# ============================================================
# Blood Banking System — Frontend Environment Variables
# Copy this to .env and update for your environment.
# NEVER commit .env to git.
# ============================================================

# API base URL
# In development the Vite proxy rewrites /api → http://localhost:8081/api
# so you only need to change this if your backend runs on a different port.
VITE_API_URL=/api
